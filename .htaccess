<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Direct all requests to the public directory unless already pointing to it
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
